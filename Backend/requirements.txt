annotated-doc==0.0.4
annotated-types==0.7.0
anthropic>=0.19.0
# Floor history: 0.2.110 -> 0.2.128 because ClaudeAgentOptions.max_buffer_size
# was not measured below it (image-carrying sessions drop at 1 MiB without it,
# see claude_sdk_session._SDK_STDOUT_LIMIT_BYTES). 25 Sep 2026: 0.2.159, the
# newest release (bundles Claude Code 2.1.281, declares mcp>=1.23,<3); the full
# Backend suite passes on it.
claude-agent-sdk>=0.2.159
anyio==4.12.1
bcrypt>=4.0.0
keyring>=25.0.0
certifi==2026.1.4
cffi==2.0.0
charset-normalizer==3.4.4
click==8.1.8
cryptography==46.0.3
distro==1.9.0
docstring_parser==0.17.0
ecdsa==0.19.1
exceptiongroup==1.3.1
fastapi==0.128.0
google-generativeai>=0.5.0
google-genai>=1.0.0
google-auth>=2.47.0
googleapis-common-protos>=1.72.0
h11==0.16.0
httpcore==1.0.9
httplib2==0.31.1
httpx==0.28.1
# 3.20: httpx2 2.13 (mcp 2.x, anthropic) requires idna>=3.18.
idna==3.20
jiter==0.12.0
ollama==0.6.1
openai>=1.20.0
proto-plus==1.27.0
protobuf==5.29.5
pyasn1==0.6.1
pyasn1_modules==0.4.2
pycparser==2.23
pydantic==2.12.5
pydantic_core==2.41.5
pyparsing==3.3.2
python-dotenv==1.2.1
python-jose==3.5.0
python-multipart==0.0.20
requests==2.32.5
rsa==4.9.1
six==1.17.0
typing_extensions==4.15.0
uritemplate==4.2.0
urllib3==2.6.3
uvicorn==0.39.0
websockets==15.0.1
# KALDIRILDI (2026-07-27): numpy, faiss-cpu, sentence-transformers.
# Gerekçe: tüm repoda tek bir `import faiss` / `import sentence_transformers` /
# `import numpy` yok (grep ile doğrulandı, Backend/ dışı dahil). sentence-transformers
# torch+transformers+scikit-learn+scipy zincirini getiriyordu (~2GB kurulum, geniş
# tedarik zinciri yüzeyi). numpy'ı zorunlu gereksinen tek paketler bunlardı; kalan
# bağımlılıklarda numpy yalnızca opsiyonel extra olarak geçiyor (openai[datalib] vb.).
# Yeniden gerekirse (vektör arama / embedding) birlikte geri eklenmeli.
# Offline speech-to-text for the dictation button (Burak, 2 Sep 2026).
# The wheel carries libvosk.dll plus three MinGW runtime DLLs; `collect_all('vosk')`
# in backend.spec is REQUIRED, see the note there.
# srt and tqdm are vosk's own runtime dependencies and are pinned here so the frozen
# build cannot drift; its other three (cffi, requests, websockets) are already pinned
# above and are deliberately NOT repeated.
# 0.3.45 published no macOS wheel (PyPI, 3 Sep 2026: linux + win_amd64 only), so the
# macOS build pins the last release that did (0.3.44, universal2). Same runtime
# dependencies on both. Measured by the v3.1.0 release run: the macOS job failed
# at pip with "No matching distribution found for vosk==0.3.45".
vosk==0.3.45; sys_platform != "darwin"
vosk==0.3.44; sys_platform == "darwin"
srt==3.5.3
tqdm==4.70.0
# tqdm's own Windows-only runtime dependency (platform_system == "Windows"); without
# an exact pin a later colorama could enter the frozen installer with no diff here
# (audit probe, 3 Sep 2026).
colorama==0.4.6
# MCP SDK 2.x (25 Sep 2026, owner-approved together with its two new packages,
# httpx2 and mcp-types). The port: tools/unity_mcp_tools.py (McpError ->
# MCPError, streamable_http_client over an httpx2.AsyncClient, snake_case
# result fields, InputRequiredResult reported as an error) and the unityai
# server (mcp.server.fastmcp -> mcp.server.mcpserver.MCPServer).
# claude-agent-sdk 0.2.159 declares mcp<3.
# The separate `fastmcp` package was dropped here (25 Sep 2026): nothing in the
# backend imports it and the full Backend suite passes with `import fastmcp`
# blocked. (fastmcp 3.x also caps mcp<2.)
mcp>=2.2.0,<3
tomli
Pillow>=10.0.0
pywin32>=306; sys_platform == "win32"
